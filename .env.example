# Copy to .env locally. Production values live in Vercel → Settings → Environment Variables.
# Anon key: Supabase → Project Settings → API → Project API keys → anon public
VITE_SUPABASE_URL=https://nxaidsisoauzhpdkaixm.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional: pull from Vercel after `npm run vercel:link`
# npx vercel env pull .env.local
